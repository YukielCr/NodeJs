// Use the arrow funtion
// Debo de estudiar mas esta parte

// to create a new array with "Hello" before each name
const names=["a","b","c"]
names
const newNames=names.map(name=> `Hello ${name}`)
newNames